<%= form_for(@cv) do |f| %>
    <% if @cv.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@cv.errors.count, "error") %> prohibited this cv from being saved:</h2>

          <ul>
            <% @cv.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <strong>Naslov:</strong>
    <div>
      <%= f.text_field :personal_skills %>
    </div>
<br>
    <div>
      <%= f.text_area :additional_information, :class => 'ckeditor' %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit %>
    </div>

<% end %>
